from setuptools import setup, find_packages

setup(
    name='SimpleSpinner',
    version='0.0.1',
    description='A simple spinner for Python',
    author='Avolord',
    author_email='the.avo.lord@gmail.com',
    packages=find_packages(),
    install_requires=[
        'colorama'
    ],
)